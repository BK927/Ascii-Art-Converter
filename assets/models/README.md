# Model Assets

ONNX model files are not committed to this repository.

For local release packaging, place the files named in
`assets/model_catalog.json` here or keep the research copies under
`target/research/ai-lineart/models/`. The Windows packaging script will copy
them into the release `models/` directory.
