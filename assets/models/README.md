# Model Assets

ONNX model files are not committed to this repository.

For local testing, place files named in `assets/model_catalog.json` here or keep
the research copies under `target/research/ai-lineart/models/`.

Release packages do not bundle these model files. Optional third-party models
are installed from the AA Converter model mirror listed in
`assets/model_catalog.json`.
